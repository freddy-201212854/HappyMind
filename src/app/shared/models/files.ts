/**
  * Enumerable for controlling constant values of local files
  */
export const enum GeneralFiles {
  register = "register.json",
  login = "login.json",
  evaluate = "evaluate.json"
}

/**
  * Enumerable to control local file constant values for oil palm
  */
export const enum PalmaFiles {
  lote = "lote_palma.json",
  mantenimiento = "mantenimiento.json",
}
